CREATE TABLE usuarios(
	id INTEGER PRIMARY KEY,
	email TEXT NOT NULL,
	password TEXT NOT NULL,
	categoría TEXT NOT NULL
);

INSERT INTO usuarios VALUES (1,'','','Cliente');
INSERT INTO usuarios VALUES (2,'','','Repartidor');
INSERT INTO usuarios VALUES (3,'','','Administrador');

SELECT * FROM usuarios

UPDATE usuarios
SET
    password = '';
WHERE id = ;

DELETE FROM usuarios
WHERE id = ;
